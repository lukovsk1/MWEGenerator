package slice;

public class SingleCharacterCodeSlice extends ACodeSlice<Byte> {

  public SingleCharacterCodeSlice(String path, byte content, int sliceNumber) {
    super(path, content, sliceNumber);
  }
}
